{
  "name": "wasm4-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build:all": "npm --workspaces --if-present run build",
    "clean:all": "npm --workspaces --if-present run clean",
    "postinstall": "check-node-version --package"
  },
  "engines": {
    "node": "^16.13.0",
    "npm": "^8.0.0"
  },
  "author": "Bruno Garcia <b@aduros.com>",
  "license": "ISC",
  "homepage": "https://wasm4.org",
  "bugs": "https://github.com/aduros/wasm4/issues",
  "repository": {
    "type": "git",
    "url": "https://github.com/aduros/wasm4.git"
  },
  "workspaces": [
    "devtools/web",
    "runtimes/web",
    "site",
    "cli"
  ],
  "devDependencies": {
    "check-node-version": "^4.2.1"
  }
}
